# Optional direnv integration: auto-enters the Nix devShell on `cd` into the
# repo (requires `direnv allow`). This is purely a convenience — the toolchain
# also works with a plain `nix develop`; direnv is NOT required.
use flake
